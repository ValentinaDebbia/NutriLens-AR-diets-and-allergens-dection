import SwiftUI

@main
struct NutriLensApp: App {
    var body: some Scene {
        WindowGroup {
            // MARK: RootView è la prima schermata dell’app.
           // qui parte tutta la logica di onboarding → success → scanner.
            RootView()
        }
    }
}
